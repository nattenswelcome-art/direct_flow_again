# 🤖 Telegram Keywords Bot

Telegram-бот для получения ключевых слов и частотности из Яндекс.Wordstat API.

## 📋 Описание

Бот принимает от пользователя список ключевых слов и возвращает Excel-файл с данными:
- Ключевое слово
- Частотность (опционально)
- Источник данных (mock/yandex)
- Дата создания

**Особенности:**
- ✅ Работает БЕЗ токенов Яндекса (использует Mock-провайдер для тестов)
- ✅ Поддержка реального Яндекс.Wordstat API
- ✅ Автоматический выбор провайдера
- ✅ 20 unit-тестов (100% покрытие core-логики)
- ✅ Линтер ruff (код соответствует стандартам)

## 🛠 Требования

- Python 3.11 или выше
- pip (менеджер пакетов Python)
- Telegram-бот токен (получить у [@BotFather](https://t.me/botfather))
- (Опционально) OAuth токен Яндекса для реальных данных

## 📦 Установка

### Шаг 1: Клонировать репозиторий (или перейти в папку проекта)

```bash
cd "/Users/tikhomirovevg/Директ Flow again"
```

### Шаг 2: Установить зависимости

```bash
python3 -m pip install -e ".[dev]"
```

Эта команда установит:
- **aiogram** — фреймворк для Telegram-ботов
- **pandas** + **openpyxl** — создание Excel-файлов
- **httpx** — HTTP-клиент для API-запросов
- **pytest** — тестирование
- **ruff** — линтер и форматтер

## ⚙️ Настройка

### Шаг 1: Создать файл `.env`

```bash
cp .env.example .env
```

### Шаг 2: Открыть `.env` и заполнить токены

```env
# Обязательно: Токен Telegram бота (получить у @BotFather)
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Опционально: Токен Яндекса (если хотите реальные данные)
# YANDEX_OAUTH_TOKEN=your_yandex_oauth_token_here

# Опционально: Логин клиента Яндекса
# YANDEX_CLIENT_LOGIN=your_client_login_here
```

### Как получить Telegram Bot Token:

1. Открыть Telegram
2. Найти [@BotFather](https://t.me/botfather)
3. Отправить команду `/newbot`
4. Следовать инструкциям
5. Скопировать токен вида `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`
6. Вставить в файл `.env`

### Как получить Яндекс OAuth Token (опционально):

1. Перейти на [OAuth.Yandex.ru](https://oauth.yandex.ru/)
2. Зарегистрировать приложение
3. Получить токен с правами `direct:api`
4. Вставить в файл `.env`

> **Примечание:** Без токена Яндекса бот будет работать с Mock-провайдером (тестовые данные).

## 🚀 Запуск бота

```bash
python3 main.py
```

**Ожидаемый вывод:**
```
🎭 Используется Mock Provider (тестовые данные)
🚀 Бот запускается с провайдером: mock
✅ Бот успешно запущен!
```

Теперь откройте Telegram и найдите вашего бота!

### Команды бота:

- `/start` — начать работу
- `/cancel` — отменить текущую операцию

### Пример использования:

1. Отправьте боту `/start`
2. Отправьте список ключевых слов:
   ```
   купить iPhone
   купить Samsung
   смартфон недорого
   ```
3. Выберите кнопку "С частотностью" или "Без частотности"
4. Получите Excel-файл с результатами!

## 🧪 Тестирование

### Запустить все тесты:

```bash
python3 -m pytest tests/ -v
```

**Ожидаемый результат:**
```
============================== 20 passed in 0.44s ==============================
```

### Запустить тесты с покрытием кода:

```bash
python3 -m pytest tests/ --cov=src --cov-report=term-missing
```

### Запустить линтер:

```bash
python3 -m ruff check src/ main.py tests/
```

### Отформатировать код:

```bash
python3 -m ruff format src/ main.py tests/
```

## 📁 Структура проекта

```
.
├── main.py                 # Точка входа (запуск бота)
├── .env                    # Конфигурация (токены) — НЕ коммитить!
├── .env.example            # Шаблон конфигурации
├── .gitignore              # Игнорируемые файлы Git
├── pyproject.toml          # Зависимости и настройки проекта
├── README.md               # Документация (этот файл)
│
├── src/                    # Исходный код
│   ├── __init__.py
│   ├── config.py           # Загрузка .env и выбор провайдера
│   ├── models.py           # Модели данных (KeywordRow)
│   │
│   ├── bot/                # Telegram бот
│   │   ├── handlers.py     # Обработчики команд и сообщений
│   │   ├── keyboards.py    # Inline-клавиатуры
│   │   └── states.py       # FSM состояния
│   │
│   ├── providers/          # Провайдеры данных
│   │   ├── base.py         # Абстрактный базовый класс
│   │   ├── mock_provider.py        # Mock (тестовые данные)
│   │   └── yandex_provider.py      # Яндекс.Wordstat API
│   │
│   └── utils/              # Утилиты
│       ├── parser.py       # Парсинг ключевых слов
│       └── excel_exporter.py       # Экспорт в Excel
│
└── tests/                  # Тесты
    ├── test_parser.py
    ├── test_excel_exporter.py
    └── test_mock_provider.py
```

## 🔧 Troubleshooting (Решение проблем)

### Проблема: `ModuleNotFoundError: No module named 'aiogram'`

**Решение:**
```bash
python3 -m pip install -e ".[dev]"
```

### Проблема: `Unauthorized: bot token is invalid`

**Решение:**
- Проверьте токен в файле `.env`
- Убедитесь, что нет лишних пробелов/кавычек
- Получите новый токен у @BotFather

### Проблема: Бот не отвечает

**Решение:**
- Проверьте, что бот запущен (`python3 main.py`)
- Проверьте интернет-соединение
- Попробуйте отправить `/start`

### Проблема: `Conflict: terminated by other getUpdates request`

**Решение:**
- Остановите другие экземпляры бота (нажмите `Ctrl+C`)
- Только один бот может работать с одним токеном

### Проблема: Тесты не запускаются

**Решение:**
```bash
# Установите pytest, если его нет
python3 -m pip install pytest pytest-asyncio

# Запустите из корневой папки проекта
python3 -m pytest tests/ -v
```

## 📊 Провайдеры данных

### Mock Provider (по умолчанию)

- Используется, если нет `YANDEX_OAUTH_TOKEN`
- Генерирует детерминированные данные (одна фраза → одна частота всегда)
- Не требует интернета и токенов
- Идеально для разработки и тестов

### Yandex Provider

- Используется, если есть `YANDEX_OAUTH_TOKEN`
- Получает реальные данные из Яндекс.Wordstat API
- Требует активного токена с правами `direct:api`
- Workflow: CreateReport → Poll → GetData → DeleteReport

## 🤝 Для разработчиков

### Добавление нового провайдера:

1. Создать класс, наследующий `WordstatProvider`
2. Реализовать методы:
   - `async def get_keywords(phrases, with_frequency)`
   - `@property def name()`
3. Добавить в `config.py` логику выбора

### Запуск в dev-режиме:

```bash
# Автоматический перезапуск при изменении кода
pip install watchdog
watchmedo auto-restart -p "*.py" -R python3 main.py
```

## 📝 Лицензия

MIT License — свободно используйте в своих проектах!

## 👤 Автор

Создано с помощью Claude Sonnet 4.5 в режиме Tutor для новичков.

## 🙋 Вопросы?

Если что-то не работает:
1. Прочитайте раздел Troubleshooting выше
2. Проверьте, что все зависимости установлены
3. Убедитесь, что `.env` файл заполнен корректно

---

**Удачи в использовании бота! 🚀**
